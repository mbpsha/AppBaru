name: uasapkmob
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  shared_preferences: ^2.3.3
  provider: ^6.1.2
  intl: ^0.19.0
  webview_flutter: ^4.10.0
  flutter_secure_storage: ^8.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/dashboardAdmin/
    - assets/dashboardAdmin/logo-ngundur.png
